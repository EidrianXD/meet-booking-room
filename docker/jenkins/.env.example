# Credenciais do admin do Jenkins (bootstrap via JCasC).
# Copie para .env e troque para algo forte.
JENKINS_ADMIN_ID=admin
JENKINS_ADMIN_PASSWORD=troque-isso

# Porta exposta no host pela UI do Jenkins.
JENKINS_PORT=8081
